# Variation Providers

A variation provider contributes controlled variation axes only.

It must not:
- alter Enter → Enclose → Guide → Reveal,
- create a second primary hook,
- replace the hidden-window rule,
- introduce provider-specific prompt syntax.
